#!/usr/bin/env bash

echo "iCheck is running..."

while true; do
    if iChecker; then
        echo -ne 1,
        date -Iseconds
        if [ -z "$@" ]; then
            :
        else
            alarm
        fi
    else
        echo -ne 0,
        date -Iseconds
    fi
    sleep 3
done
