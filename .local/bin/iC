#!/usr/bin/env bash

echo "iCheck is running..."

while true; do
    if iChecker; then
        echo 'Internet is up'
        if [ -z "$@" ]; then
            :
        else
            alarm
        fi
    else
        echo 'Internet is down'
    fi
    sleep 10
done
