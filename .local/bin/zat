#!/usr/bin/sh

if [ -z "$@" ]; then
	fileToView="$(unbuffer bfs -nocolor . -regex '.*\.pdf' -type f | fzf --bind 'return:execute(zathura {})+abort')"
	# echo "Opening Zathura on $fileToView"
	# zathura "$fileToView"
else
	zathura "$@"
fi
